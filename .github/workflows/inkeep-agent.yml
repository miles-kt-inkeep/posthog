name: Inkeep Agent

on:
    issue_comment:
        types: [created]
    
    pull_request_review_comment:
        types: [created]
    
    pull_request_review:
        types: [submitted]

    pull_request:
        types: [closed]

permissions:
  contents: read
  pull-requests: read
  id-token: write

jobs:
    trigger-agent:
        if: |
            !contains(github.actor, '[bot]') &&
            (
              (github.event_name == 'pull_request' && github.event.pull_request.merged == true) ||
              (github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body, '@inkeep')) ||
              (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@inkeep')) ||
              (github.event_name == 'pull_request_review' && contains(github.event.review.body || '', '@inkeep'))
            )
        runs-on: ubuntu-latest
        steps:
            - name: Trigger Inkeep Agent
              uses: inkeep/inkeep-agents-action@v0.3.0
              with:
                  trigger-url: https://api.agents.inkeep.com/run/tenants/default/projects/posthog-content-dev/agents/docs-writer-agent/triggers/uuti74bshs6b68cjk2wpf
